return {
  -- {
  --   -- TODO: Disable , and ; maps (they go back/forward from f/F/t/T, but you can also just keep hitting f/F/t/T)
  --   --       - Instead, maybe add back , as "clear search highlight + NoiceDismiss"
  --   --       - I may just need to set my own keymaps - it seems flash.nvim doesn't override if already set:
  --   --             ~/.local/share/nvim/lazy/flash.nvim/lua/flash/plugins/char.lua +109
  --   "folke/flash.nvim",
  --   opts = { },
  -- },
}

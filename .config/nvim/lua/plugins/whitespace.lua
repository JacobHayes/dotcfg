return {
  { "NMAC427/guess-indent.nvim" }, -- Detect indents (shiftwidth, tabstop, etc)
  { "kaplanz/retrail.nvim", opts = { hlgroup = "errorMsg" } }, -- Highlights trailing spaces + deletes on write
}
